package com.phasepal.phasepulse.command;

import com.phasepal.phasepulse.PhasePulse;

/**
 * NeoForge port placeholder.
 * Client command registration is pending migration from Fabric command callbacks.
 */
public final class PalCommand {
    private PalCommand() {
    }

    public static void register() {
        PhasePulse.LOGGER.info("PalCommand is temporarily disabled during NeoForge setup.");
    }
}
